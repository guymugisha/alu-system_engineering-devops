#!/usr/bin/env bash
# Configures a new Ubuntu machine to add X-Served-By header in Nginx response
apt-get update -y
apt-get install -y nginx

sed -i "s/# server_tokens off;/# server_tokens off;\n\tadd_header X-Served-By $(hostname) always;/" /etc/nginx/nginx.conf

service nginx restart
